<h1>All tasks:</h1>
<div>
  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= link_to task.title, task_path(task) %>
      </li>
    <% end %>
  </ul>
</div>

<div>
  <a href="/tasks/new" class="btn btn-primary"> +  Add a task</a>
</div>
